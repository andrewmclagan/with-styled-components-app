import { ReactNode } from 'react';
export declare type SelectGroupProps = {
    children: ReactNode;
    isConstrained?: boolean;
    label?: string;
};
export default function SelectGroup(props: SelectGroupProps): JSX.Element;
