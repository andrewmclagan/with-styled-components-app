import { ReactNode } from 'react';
import { AvatarProps } from '../../../components/public/Avatar/Avatar';
export declare type AvatarGroupProps<T> = {
    children: React.ReactElement | React.ReactElement[];
    max?: number;
    renderName?: (props: T) => ReactNode;
};
export default function AvatarGroup<T = AvatarProps>(props: AvatarGroupProps<T>): JSX.Element;
