/// <reference types="react" />
import moment from 'moment';
import { dateTimeSizes } from '../../../constants/sizes';
export declare const variants: {
    noWrap: "noWrap";
};
export declare type DateTimeProps = {
    prefix?: string;
    size?: keyof typeof dateTimeSizes;
    testHook?: string;
    value: Date | moment.Moment;
    variant?: keyof typeof variants;
    withStatus?: boolean;
};
export default function DateTime(props: DateTimeProps): JSX.Element;
