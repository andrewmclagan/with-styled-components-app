import { ValueOf } from '../../types/helpers';
import { colors, names } from './colors';
export declare type DropColor<T extends string> = T extends `color${infer Rest}` ? Rest : never;
export declare type Key = keyof typeof colors;
export declare type Name = keyof typeof names;
export declare type ShortKey = DropColor<Key>;
export declare type ShortName = DropColor<Name>;
export declare type Value = ValueOf<typeof colors>;
export declare type Colors = Record<Key, Value>;
export declare type ColorShades = {
    lighter: Key;
    lightest: Key;
    root: Key;
};
