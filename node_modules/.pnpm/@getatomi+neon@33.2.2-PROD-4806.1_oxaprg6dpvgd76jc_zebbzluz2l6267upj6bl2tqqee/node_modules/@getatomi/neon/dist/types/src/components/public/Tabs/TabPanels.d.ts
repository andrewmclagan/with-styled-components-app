import { ReactNode } from 'react';
export declare type TabPanelsProps = {
    /**
     * `TabPanels` expects multiple `TabPanel` elements as children.
     */
    children: ReactNode;
};
export default function TabPanel(props: TabPanelsProps): JSX.Element;
