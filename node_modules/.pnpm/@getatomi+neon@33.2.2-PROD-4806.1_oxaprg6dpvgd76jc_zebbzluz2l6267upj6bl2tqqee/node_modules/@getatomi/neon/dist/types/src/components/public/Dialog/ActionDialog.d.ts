import { ComponentProps, ReactElement, ReactNode, SyntheticEvent } from 'react';
import Button from '../../../components/public/Button/Button';
export declare const variants: {
    compact: "compact";
};
export declare type ActionDialogProps = {
    appElement: HTMLElement;
    cancelButtonLabel: string;
    children: ReactNode;
    closeAssistiveText: string;
    form: ReactElement;
    heading?: string;
    isOpen: boolean;
    onClose: (e?: SyntheticEvent) => void;
    submitButtonProps: ComponentProps<typeof Button>;
    testHook?: string;
    variant?: keyof typeof variants;
};
export default function ActionDialog(props: ActionDialogProps): JSX.Element;
