import { SensorContext } from '@dnd-kit/core';
export declare const droppableCoordinateGetter: (answersBankId: string) => (event: KeyboardEvent, { context, }: {
    context: SensorContext;
}) => {
    x: number;
    y: number;
} | undefined;
