import { ReactNode } from 'react';
export declare type DefinitionProps = {
    children: ReactNode;
    /**
     * The definition to be displayed in the tooltip.
     */
    definition: ReactNode;
};
export default function Definition(props: DefinitionProps): JSX.Element;
