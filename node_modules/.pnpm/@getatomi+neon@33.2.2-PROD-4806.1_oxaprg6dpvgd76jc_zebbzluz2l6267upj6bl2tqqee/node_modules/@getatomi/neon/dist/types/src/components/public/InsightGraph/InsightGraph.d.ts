import { ComponentProps, ReactNode } from 'react';
import { XAxis } from 'recharts';
declare type TooltipPayload = {
    color: string;
    dataKey: 'y' | 'yComparison';
    name: string;
    value: number;
};
declare type TooltipPayloadTuple = [TooltipPayload | null | undefined, TooltipPayload | null | undefined];
export declare type Series = {
    data: ReadonlyArray<{
        x: string | number;
        y: number;
    } | null | undefined>;
    id: string;
};
export declare type TooltipContentRenderer = (args: {
    label?: string;
    payload: TooltipPayloadTuple;
}) => ReactNode;
export declare type InsightGraphProps = {
    data: [Series, Series];
    isEmbedded?: boolean;
    renderTooltipContent: TooltipContentRenderer;
    renderValue: (arg0: number) => ReactNode;
    testHook?: string;
    xAxisProps?: ComponentProps<typeof XAxis>;
};
export default function InsightGraph(props: InsightGraphProps): JSX.Element;
export {};
