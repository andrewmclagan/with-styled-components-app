import { ReactNode } from 'react';
import Icon from '../../../components/public/Icon/Icon';
import { Value as ColorValue } from '../../../constants/tokens/colors.types';
export declare const variants: {
    large: "large";
    narrow: "narrow";
};
export declare type MenuItemOption = {
    readonly color?: ColorValue;
    readonly iconName?: JSX.LibraryManagedAttributes<typeof Icon, React.ComponentProps<typeof Icon>>['name'];
    readonly isActive?: boolean;
    readonly isDanger?: boolean;
    readonly isDisabled?: boolean;
    readonly variant?: keyof typeof variants;
    readonly withActiveIcon?: boolean;
    readonly withTruncation?: boolean;
};
export declare type MenuItemProps = MenuItemOption & {
    children: ReactNode;
    testHook?: string;
};
export default function MenuItem(props: MenuItemProps): JSX.Element;
