import { ReactNode } from 'react';
export declare const variants: {
    error: "error";
    warning: "warning";
};
export declare type HelpTextProps = {
    children: ReactNode;
    id?: string;
    marginTop?: 'spacingNone';
    testHook?: string;
    variant?: keyof typeof variants;
};
export default function HelpText(props: HelpTextProps): JSX.Element;
