import { ReactNode } from 'react';
import { BoxProps } from '../../../components/public/Box/Box';
declare type Bar = {
    ariaLabel: string;
    color?: BoxProps['backgroundColor'];
    onClick?: () => void;
    tooltip?: ReactNode;
    value?: number;
};
export declare const variants: {
    readonly chunky: "chunky";
    readonly large: "large";
};
export declare type ProgressBarCommonProps = {
    color?: Bar['color'];
    max?: number;
    renderLabel?: (progress: string) => ReactNode;
    role?: 'meter' | 'progressbar';
    testHook?: string;
    variant?: keyof typeof variants;
    withLabel?: boolean;
};
declare type ValuesProps = {
    ariaLabel: string;
    value?: number;
    values?: never;
} | {
    ariaLabel?: never;
    value?: never;
    values: Array<Bar>;
};
export declare type ProgressBarProps = ProgressBarCommonProps & ValuesProps;
export default function ProgressBar(props: ProgressBarProps): JSX.Element;
export {};
