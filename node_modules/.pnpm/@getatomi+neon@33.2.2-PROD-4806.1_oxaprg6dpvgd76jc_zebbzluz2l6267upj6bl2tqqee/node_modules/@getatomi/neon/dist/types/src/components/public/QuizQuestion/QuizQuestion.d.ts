import { ComponentType, ReactNode, Ref } from 'react';
import { IllustrationProps } from '../../../components/public/Illustration/Illustration';
import { AppLinkProps } from '../../../../test/stubs';
export declare const variants: {
    readonly correct: "correct";
    readonly incorrect: "incorrect";
    readonly partlyCorrect: "partlyCorrect";
    readonly skipped: "skipped";
};
export declare type Variant = keyof typeof variants;
export declare type QuizQuestionProps = {
    copyright?: string;
    explanation?: string;
    feedback?: ReactNode;
    /**
     * A ref passed to feedback div. This can allow the consumer to scroll
     * feedback into view.
     */
    feedbackRef?: Ref<HTMLDivElement>;
    illustrationName?: IllustrationProps['name'];
    /**
     * Defaults to `true`. Set it to `false` to prevent the feedback from being
     * automatically read by screen readers.
     */
    isFeedbackAnnounced?: boolean;
    /**
     * The labelId is set on the title wrapper. This is used to label questions
     * with an input such as exact answer questions.
     */
    labelId?: string;
    linkComponent?: ComponentType<AppLinkProps>;
    meta?: ReactNode;
    source?: string;
    testHook?: string;
    title: ReactNode;
    variant?: Variant;
};
declare type Props = QuizQuestionProps & {
    children?: ReactNode;
};
export default function QuizQuestion(props: Props): JSX.Element;
export {};
