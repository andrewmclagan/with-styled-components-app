import sizes from '../constants/sizes';
import { spacing } from '../constants/tokens/spacing';
export declare type SizeKey = keyof typeof sizes;
export declare type SpacingKey = keyof typeof spacing;
export declare function getSpacing(size: SizeKey): string;
