import { ReactNode } from 'react';
import { ModalProps } from './Modal';
export declare type ModalHeaderProps = Pick<ModalProps, 'onClose' | 'size' | 'variant'> & {
    children: ReactNode;
};
export default function ModalHeader(props: ModalHeaderProps): JSX.Element;
