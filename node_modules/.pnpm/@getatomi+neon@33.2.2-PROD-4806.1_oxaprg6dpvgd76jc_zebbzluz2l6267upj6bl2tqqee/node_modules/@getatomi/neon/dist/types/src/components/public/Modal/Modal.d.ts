import { ReactNode, RefObject } from 'react';
export declare const sizes: {
    readonly small: "small";
    readonly medium: "medium";
    readonly fullscreen: "fullscreen";
};
export declare const defaultSize: "medium";
export declare const scroll: {
    readonly body: "body";
    readonly page: "page";
};
export declare const variants: {
    readonly view: "view";
};
export declare type ModalProps = {
    actions?: ReactNode;
    /**
     * The id that refers to a heading inside the modal. Provide when the `heading` prop isn't set.
     */
    ariaLabelledBy?: string;
    children: ReactNode;
    heading?: ReactNode;
    /**
     * A ref to a focusable element inside the modal. (defaults to the first focusable element inside the modal)
     */
    initialFocusRef?: RefObject<any>;
    isOpen: boolean;
    onClose: () => void;
    scroll?: keyof typeof scroll;
    size?: keyof typeof sizes;
    variant?: keyof typeof variants;
};
export default function Modal(props: ModalProps): JSX.Element;
