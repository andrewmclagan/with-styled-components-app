import { ReactNode, MouseEventHandler } from 'react';
import { InsightGraphProps } from '../../../components/public/InsightGraph/InsightGraph';
export declare type InsightProps = {
    description: ReactNode;
    diffDescription: ReactNode;
    diffTooltip?: ReactNode;
    diffValue: number;
    graphProps?: InsightGraphProps;
    isActive?: boolean;
    onClick?: MouseEventHandler;
    testHook?: string;
    title: ReactNode;
    value: ReactNode;
};
declare function Insight(props: InsightProps): JSX.Element;
declare namespace Insight {
    var Loader: typeof InsightLoader;
}
export default Insight;
declare function InsightLoader(): JSX.Element;
