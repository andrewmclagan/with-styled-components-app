import { ReactNode } from 'react';
import { BoxProps } from '../../../components/public/Box/Box';
declare type Variant = keyof typeof variants;
declare const variants: {
    readonly chunky: "chunky";
    readonly dashed: "dashed";
    readonly filled: "filled";
};
export declare const defaultStyles: BoxProps;
export declare type TagProps = Pick<BoxProps, 'children' | 'color'> & {
    fillColor?: BoxProps['backgroundColor'];
    tooltip?: ReactNode;
    variant?: Variant;
};
export default function Tag(props: TagProps): JSX.Element;
export {};
