/// <reference types="react" />
export declare const variants: {
    fade: "fade";
    smallShadow: "smallShadow";
};
export declare type HorizontalScrollProps = {
    children: JSX.Element;
    /**
     * Forwards the scroll position (top/left) as a `scrollPosition` prop to the
     * children element.
     */
    forwardScrollPosition?: boolean;
    variant?: keyof typeof variants;
};
declare type ScrollPosition = {
    left: number;
    top: number;
};
export declare const setupScrollHandling: (container: HTMLElement, wrapper: HTMLElement, setIsScrollable: (isScrollable: boolean) => void, setIsAtEnd: (isAtEnd: boolean) => void, setScrollPosition: (scrollPosition: ScrollPosition) => void) => () => void;
export default function HorizontalScroll(props: HorizontalScrollProps): JSX.Element;
export {};
