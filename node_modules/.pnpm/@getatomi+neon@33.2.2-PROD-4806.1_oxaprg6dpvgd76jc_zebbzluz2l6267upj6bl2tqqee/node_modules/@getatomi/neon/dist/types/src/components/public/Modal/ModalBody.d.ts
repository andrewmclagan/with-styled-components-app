import { ReactNode } from 'react';
import { ModalProps } from './Modal';
declare type ModalBodyProps = Pick<ModalProps, 'size' | 'scroll'> & {
    children: ReactNode;
};
export default function ModalBody(props: ModalBodyProps): JSX.Element;
export {};
