import { ReactNode } from 'react';
import Strength from '../../../components/public/Strength/Strength';
export declare type StrengthInfoProps = {
    diffs: ReadonlyArray<{
        render: (arg0: ReactNode) => ReactNode;
        value: number;
    }>;
    renderStrengthLabel: (arg0: ReactNode) => ReactNode;
    renderSuccessRate: (arg0: ReactNode) => ReactNode;
    strengthLabels: [string, string, string, string];
    strengthProps: JSX.LibraryManagedAttributes<typeof Strength, React.ComponentProps<typeof Strength>>;
    successRate: number;
    testHook?: string;
};
export default function StrengthInfo(props: StrengthInfoProps): JSX.Element;
