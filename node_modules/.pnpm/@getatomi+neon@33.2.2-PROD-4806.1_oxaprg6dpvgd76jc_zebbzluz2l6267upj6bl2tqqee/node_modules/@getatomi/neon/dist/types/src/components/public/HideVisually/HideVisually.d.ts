import { ReactNode } from 'react';
export declare type HideVisuallyProps = {
    /**
     * The element type for the container.
     * @default 'span'
     */
    as?: string;
    children: ReactNode;
};
export default function HideVisually(props: HideVisuallyProps): JSX.Element;
