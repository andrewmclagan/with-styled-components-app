import { ComponentProps, ReactNode } from 'react';
import Button from '../../../components/public/Button/Button';
import Heading from '../../../components/public/Heading/Heading';
export declare const variants: {
    coloredBackground: "coloredBackground";
};
export declare type EmptyStateProps = {
    ctaProps?: ComponentProps<typeof Button>;
    customCta?: ReactNode;
    description: ReactNode;
    descriptionInfo?: ReactNode;
    header?: ReactNode;
    headingProps?: React.ComponentProps<typeof Heading>;
    helpInfo?: ReactNode;
    isGutterless?: boolean;
    testHook?: string;
    variant?: keyof typeof variants;
};
export default function EmptyState(props: EmptyStateProps): JSX.Element;
