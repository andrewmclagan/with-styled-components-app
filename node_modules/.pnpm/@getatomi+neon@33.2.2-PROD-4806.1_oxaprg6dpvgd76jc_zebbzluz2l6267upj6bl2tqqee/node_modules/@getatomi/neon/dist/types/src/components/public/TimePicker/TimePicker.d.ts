/// <reference types="react" />
import { SelectFieldProps } from '../../../components/public/SelectField/SelectField';
export declare type Range = {
    max: string;
    min: string;
};
export declare type TimePickerProps = Omit<SelectFieldProps, 'options'> & {
    inputFormat: string;
    range?: Range;
};
export default function TimePicker(props: TimePickerProps): JSX.Element;
