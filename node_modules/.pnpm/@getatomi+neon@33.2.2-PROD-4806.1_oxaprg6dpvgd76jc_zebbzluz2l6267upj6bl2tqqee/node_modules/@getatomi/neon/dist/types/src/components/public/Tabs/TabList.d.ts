import { ReactNode } from 'react';
export declare type TabListProps = {
    /**
     * Text used as the label on the tabs list.
     */
    ariaLabel: string;
    /**
     * `TabList` expects multiple `Tab` or `TabLink` elements as children.
     */
    children: ReactNode;
};
export default function TabList(props: TabListProps): JSX.Element;
