import { ReactNode } from 'react';
declare const variants: {
    readonly centered: "centered";
};
export declare type TabsProps = {
    children: ReactNode;
    /**
     * Starts the tabs at the specified index.
     */
    defaultIndex?: number;
    /**
     * Describes the activation mode when navigating a tablist with a keyboard.
     * When set to `"auto"`, a tab panel is activated automatically when a tab is
     * highlighted using arrow keys. When set to `"manual"`, the user must
     * activate the tab panel with either the `Spacebar` or `Enter` keys.
     * @default auto
     */
    keyboardActivation?: 'auto' | 'manual';
    variant?: keyof typeof variants;
};
export default function Tabs(props: TabsProps): JSX.Element;
export { default as Tab, TabProps } from './Tab';
export { default as TabLink, TabLinkProps } from './TabLink';
export { default as TabList, TabListProps } from './TabList';
export { default as TabPanel, TabPanelProps } from './TabPanel';
export { default as TabPanels, TabPanelsProps } from './TabPanels';
