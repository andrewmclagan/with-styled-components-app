import { ReactNode } from 'react';
import { sizes } from './Modal';
declare type ModalFooterProps = {
    children: ReactNode;
    size: typeof sizes.small | typeof sizes.medium;
};
export default function ModalFooter(props: ModalFooterProps): JSX.Element;
export {};
