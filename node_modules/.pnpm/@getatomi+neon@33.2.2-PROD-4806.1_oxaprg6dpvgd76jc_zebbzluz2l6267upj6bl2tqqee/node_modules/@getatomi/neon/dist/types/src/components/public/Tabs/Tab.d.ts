/// <reference types="react" />
import { TabProps as ReachTabProps } from '@reach/tabs';
export declare type TabProps = Pick<ReachTabProps, 'children' | 'index'> & {
    /**
     * Disables a tab when true. Clicking will not work and keyboard navigation
     * will skip over it.
     */
    isDisabled?: boolean;
    isSelected?: boolean;
};
export default function Tab(props: TabProps): JSX.Element;
