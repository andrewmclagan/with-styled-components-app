import { ReactNode } from 'react';
import { IconName } from '../../../components/public/Icon/Icon';
import { progressCircleSizes } from '../../../constants/sizes';
import { progressElements } from '../../../constants/allowedHtmlElements';
export declare type ProgressCircleProps = {
    ariaLabel?: string;
    backingElement?: keyof typeof progressElements;
    children?: ReactNode;
    iconName?: IconName;
    isDisabled?: boolean;
    isOverlaid?: boolean;
    label?: ReactNode;
    size?: keyof typeof progressCircleSizes;
    testHook?: string;
    threshold?: number;
    value: number;
    withCompletedState?: boolean;
    withStatus?: boolean;
};
export default function ProgressCircle(props: ProgressCircleProps): JSX.Element;
