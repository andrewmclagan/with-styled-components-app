import { ReactNode } from 'react';
import { BoxProps } from '../../../components/public/Box/Box';
import { sizingsAvatar } from '../../../constants/tokens/sizingsAvatar';
export declare const variants: {
    readonly muted: "muted";
};
export declare const avatarDefaultSize = "sizeAvatarRoot";
export declare type AvatarProps = Pick<BoxProps, 'backgroundColor'> & {
    icon?: ReactNode;
    isDecorative?: boolean;
    name?: string;
    size?: keyof typeof sizingsAvatar;
    src?: string;
    tooltip?: ReactNode;
    variant?: keyof typeof variants;
};
export default function Avatar(props: AvatarProps): JSX.Element;
