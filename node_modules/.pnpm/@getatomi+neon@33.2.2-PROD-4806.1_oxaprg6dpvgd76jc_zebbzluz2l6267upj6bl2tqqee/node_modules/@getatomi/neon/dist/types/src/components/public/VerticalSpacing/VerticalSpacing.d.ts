import { ReactNode } from 'react';
import sizes from '../../../constants/sizes';
export declare type VerticalSpacingProps = {
    children: ReactNode;
    isUpwards?: boolean;
    size?: keyof typeof sizes;
    testHook?: string;
};
export default function VerticalSpacing(props: VerticalSpacingProps): JSX.Element;
