import { ComponentProps, ReactNode, MouseEvent, KeyboardEvent } from 'react';
import ReactModal from 'react-modal';
export declare type Props = ComponentProps<typeof ReactModal> & {
    appElement: HTMLElement;
    ariaLabel?: string;
    children: ReactNode;
    className?: string;
    headingId?: string;
    isOpen: boolean;
    onClose?: (e?: MouseEvent<HTMLElement> | KeyboardEvent<HTMLElement>) => void;
    overlayClassName?: string;
};
export default function DialogBase(props: Props): JSX.Element;
