/// <reference types="react" />
import { pulsatingDotSizes } from '../../../constants/sizes';
export declare type PulsatingDotProps = {
    className?: string;
    color?: string;
    size?: keyof typeof pulsatingDotSizes;
};
export default function PulsatingDot(props: PulsatingDotProps): JSX.Element;
