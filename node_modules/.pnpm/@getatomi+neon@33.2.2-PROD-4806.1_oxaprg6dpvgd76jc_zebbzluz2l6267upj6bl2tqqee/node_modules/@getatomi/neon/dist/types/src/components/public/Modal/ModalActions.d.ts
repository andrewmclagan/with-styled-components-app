import { ReactNode } from 'react';
import { ModalProps } from './Modal';
declare type ModalActionsProps = Pick<ModalProps, 'size'> & {
    children: ReactNode;
};
export default function ModalActions(props: ModalActionsProps): JSX.Element;
export {};
