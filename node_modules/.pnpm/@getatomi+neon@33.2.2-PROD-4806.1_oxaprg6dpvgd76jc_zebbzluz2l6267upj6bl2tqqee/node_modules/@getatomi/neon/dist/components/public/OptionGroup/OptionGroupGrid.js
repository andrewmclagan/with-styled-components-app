import { jsx } from 'react/jsx-runtime';
import Box from '../Box/Box.js';

const defaultStyles = {
    display: 'grid',
    gap: 'spacingRoot',
    gridTemplateColumns: {
        base: undefined,
        mobile: 'repeat(3, 1fr)'
    }
};
const squareStyles = {
    ...defaultStyles,
    gridTemplateColumns: {
        base: 'repeat(2, 1fr)',
        mobile: 'repeat(3, 1fr)'
    }
};
function OptionGroupGrid(props) {
    const { children , variant  } = props;
    return jsx(Box, Object.assign({}, variant === 'square' ? squareStyles : defaultStyles, {
        children: children
    }), void 0);
}

export { OptionGroupGrid as default };
