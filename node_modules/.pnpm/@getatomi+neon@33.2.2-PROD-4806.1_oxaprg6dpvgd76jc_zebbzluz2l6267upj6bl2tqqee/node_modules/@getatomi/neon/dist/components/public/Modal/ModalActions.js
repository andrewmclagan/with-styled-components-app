import { jsx } from 'react/jsx-runtime';
import Flex from '../Flex/Flex.js';
import { defaultSize } from './Modal.js';

const sharedStyles = {
    gap: 'spacingLarge'
};
const smallStyles = {
    ...sharedStyles,
    justifyContent: 'flex-end'
};
const mediumStyles = {
    ...sharedStyles,
    justifyContent: {
        base: 'space-between',
        tablet: 'flex-end'
    }
};
const fullscreenStyles = {
    ...sharedStyles,
    marginLeft: 'auto'
};
const sizeStylesMapping = {
    small: smallStyles,
    medium: mediumStyles,
    fullscreen: fullscreenStyles
};
function ModalActions(props) {
    const { children , size =defaultSize  } = props;
    return jsx(Flex, Object.assign({}, sizeStylesMapping[size], {
        children: children
    }), void 0);
}

export { ModalActions as default };
