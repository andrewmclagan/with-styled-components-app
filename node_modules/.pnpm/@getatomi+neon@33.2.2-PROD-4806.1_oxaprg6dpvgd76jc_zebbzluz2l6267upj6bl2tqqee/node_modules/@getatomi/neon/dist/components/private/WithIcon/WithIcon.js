import { jsxs, jsx } from 'react/jsx-runtime';
import cx from 'classnames';
import { forwardRef } from 'react';
import { containerElements } from '../../../constants/allowedHtmlElements.js';
import Icon from '../../public/Icon/Icon.js';
import styles from './WithIcon.css.js';

const WithIcon = /*#__PURE__*/ forwardRef(function WithIcon(props, ref) {
    const { as: Component = containerElements.div , className , children , iconProps , ...otherProps } = props;
    return jsxs(Component, Object.assign({}, otherProps, {
        className: cx(styles.root, className),
        ref: ref
    }, {
        children: [
            jsx("div", Object.assign({
                className: styles.icon
            }, {
                children: jsx(Icon, Object.assign({}, iconProps, {
                    size: {
                        base: 'sizeIconSmall',
                        tablet: 'sizeIconRoot'
                    }
                }), void 0)
            }), void 0),
            children
        ]
    }), void 0);
});

export { WithIcon as default };
