import { jsx } from 'react/jsx-runtime';
import Box from '../../public/Box/Box.js';
import Tooltip from '../../public/Tooltip/Tooltip.js';

function Definition(props) {
    const { definition , children  } = props;
    return jsx(Tooltip, Object.assign({
        theme: "light-border",
        size: "large",
        trigger: "click focus",
        interactive: true,
        content: definition
    }, {
        children: jsx(Box, Object.assign({
            as: "span",
            borderBottomWidth: "borderWidthLarge",
            borderBottomStyle: "dotted",
            borderBottomColor: "colorAtomiBluesDark",
            role: "button"
        }, {
            children: children
        }), void 0)
    }), void 0);
}

export { Definition as default };
