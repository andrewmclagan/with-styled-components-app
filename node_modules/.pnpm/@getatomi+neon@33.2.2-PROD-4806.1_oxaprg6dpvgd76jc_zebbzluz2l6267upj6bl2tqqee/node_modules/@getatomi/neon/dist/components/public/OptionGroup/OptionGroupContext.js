import { useContext, createContext } from 'react';
import invariant from 'invariant';

const OptionGroupContext = /*#__PURE__*/ createContext(null);
function useOptionGroupContext() {
    const context = useContext(OptionGroupContext);
    invariant(context, 'useOptionGroupContext must be used within a OptionGroupContext.Provider.');
    return context;
}

export { OptionGroupContext, useOptionGroupContext };
