import { jsx } from 'react/jsx-runtime';
import { Children, cloneElement } from 'react';
import { TabList as TabList$1 } from '@reach/tabs';
import flattenChildren from 'react-flatten-children';
import Box from '../Box/Box.js';
import HorizontalScroll from '../HorizontalScroll/HorizontalScroll.js';
import styles from './Tabs.css.js';

function TabList(props) {
    const { ariaLabel , children , ...otherProps } = props;
    return jsx(Box, Object.assign({
        className: styles.root
    }, {
        children: jsx(HorizontalScroll, {
            children: jsx(Box, Object.assign({
                className: styles.tabListContainer
            }, {
                children: jsx(TabList$1, Object.assign({}, otherProps, {
                    "aria-label": ariaLabel,
                    className: styles.tabList
                }, {
                    children: Children.map(flattenChildren(children), (child, i)=>/*#__PURE__*/ cloneElement(child, {
                            index: i
                        })
                    )
                }), void 0)
            }), void 0)
        }, void 0)
    }), void 0);
}

export { TabList as default };
