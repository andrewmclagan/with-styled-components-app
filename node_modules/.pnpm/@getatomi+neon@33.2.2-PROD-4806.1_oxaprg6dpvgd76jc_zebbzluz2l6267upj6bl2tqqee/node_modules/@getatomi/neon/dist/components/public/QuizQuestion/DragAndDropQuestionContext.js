import { createContext, useContext } from 'react';
import invariant from 'invariant';

const DragAndDropQuestionContext = /*#__PURE__*/ createContext(undefined);
// The useDragAndDropQuestionContext() is required to communicate between the
// Markdown, which initially contains the answers (e.g. "{{detroit}}") to the
// parent component (e.g. DragAndDropQuestion).
function useDragAndDropQuestionContext() {
    const context = useContext(DragAndDropQuestionContext);
    invariant(context, 'useDragAndDropQuestionContext must be used within a DragAndDropQuestionContext.Provider. You might be using the drag-and-drop markdown syntax (e.g. `{{answer}}`) outside of a quiz question.');
    return context;
}

export { DragAndDropQuestionContext, useDragAndDropQuestionContext };
