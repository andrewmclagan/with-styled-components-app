import { jsxs, jsx } from 'react/jsx-runtime';
import cx from 'classnames';
import keyMirror from 'keymirror';
import { headingSizes } from '../../../constants/sizes.js';
import styles from './Heading.css.js';

const ranks = keyMirror({
    '1': null,
    '2': null,
    '3': null,
    '4': null,
    '5': null,
    '6': null
});
function Heading(props) {
    const { id , className , isPageTitle =false , isVisuallyHidden =false , label , rank =ranks['2'] , size =headingSizes.large1X , testHook , children ,  } = props;
    const Element = `h${isPageTitle ? ranks['1'] : ranks[rank]}`;
    return jsxs(Element, Object.assign({
        className: cx(className, {
            [styles.isPageTitle]: isPageTitle,
            [String(styles[size])]: !isPageTitle,
            [styles.isVisuallyHidden]: isVisuallyHidden,
            [styles.withLabel]: !!label
        }),
        "data-test": testHook,
        id: id
    }, {
        children: [
            jsx("span", Object.assign({
                className: styles.heading
            }, {
                children: children
            }), void 0),
            label && jsx("span", Object.assign({
                className: styles.label,
                "data-test": testHook ? `${testHook}-label` : undefined
            }, {
                children: label
            }), void 0)
        ]
    }), void 0);
}

export { Heading as default, ranks };
