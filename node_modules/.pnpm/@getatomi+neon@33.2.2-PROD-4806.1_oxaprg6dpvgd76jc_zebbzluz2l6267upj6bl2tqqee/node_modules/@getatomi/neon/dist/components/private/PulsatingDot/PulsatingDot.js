import { jsx } from 'react/jsx-runtime';
import cx from 'classnames';
import styles from './PulsatingDot.css.js';

function PulsatingDot(props) {
    const { className , color , size  } = props;
    return jsx("span", {
        className: cx(styles.root, className, size && styles[size]),
        style: color ? {
            '--color': color
        } : undefined
    }, void 0);
}

export { PulsatingDot as default };
