import { jsx } from 'react/jsx-runtime';
import { Tabs as Tabs$1 } from '@reach/tabs';
import styles from './Tabs.css.js';
import 'classnames';
import 'react';
import 'react-flatten-children';
import '../Box/Box.js';
import '../HorizontalScroll/HorizontalScroll.js';

function Tabs(props) {
    const { children , defaultIndex , keyboardActivation , variant  } = props;
    return jsx(Tabs$1, Object.assign({
        // @ts-ignore reach-ui forces the use of the TabsKeyboardActivation enum which is impratical
        keyboardActivation: keyboardActivation,
        defaultIndex: defaultIndex,
        className: variant && styles[variant]
    }, {
        children: children
    }), void 0);
}

export { Tabs as default };
