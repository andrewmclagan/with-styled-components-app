import { jsx } from 'react/jsx-runtime';
import cx from 'classnames';
import sizes from '../../../constants/sizes.js';
import Box from '../Box/Box.js';
import styles from './VerticalSpacing.css.js';

function VerticalSpacing(props) {
    const { children , isUpwards =false , size =sizes.root , testHook  } = props;
    return jsx(Box, Object.assign({
        className: cx({
            [String(styles[size])]: size && !isUpwards,
            [styles[`${size}Up`]]: isUpwards
        }),
        testHook: testHook
    }, {
        children: children
    }), void 0);
}

export { VerticalSpacing as default };
