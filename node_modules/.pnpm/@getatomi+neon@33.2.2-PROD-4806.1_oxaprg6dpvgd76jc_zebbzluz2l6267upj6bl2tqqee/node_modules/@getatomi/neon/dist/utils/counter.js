import 'core-js/modules/web.dom-collections.iterator.js';
import 'react';
import _ from 'lodash';

function counter() {
    let index = 0;
    return {
        next: ()=>++index
    };
}
function labelCounter(prefix) {
    const ctr = counter();
    return {
        next: ()=>`${prefix}-${_.padStart(`${ctr.next()}`, 3, '0')}`
    };
}

export { counter, labelCounter as default };
