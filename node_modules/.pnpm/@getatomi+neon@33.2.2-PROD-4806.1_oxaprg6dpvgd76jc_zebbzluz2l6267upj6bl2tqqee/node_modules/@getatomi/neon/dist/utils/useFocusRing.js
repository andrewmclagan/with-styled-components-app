import { useFocusRing as useFocusRing$1 } from '@react-aria/focus';

function useFocusRing() {
    const { isFocusVisible , ...rest } = useFocusRing$1();
    return {
        isFocusVisible,
        focusStyles: isFocusVisible ? {
            outlineWidth: 'borderWidthLarge1X',
            outlineColor: 'colorInteractiveFocusRing',
            outlineStyle: 'solid',
            outlineOffset: 'spacingSmall3X'
        } : undefined,
        ...rest
    };
}

export { useFocusRing as default };
